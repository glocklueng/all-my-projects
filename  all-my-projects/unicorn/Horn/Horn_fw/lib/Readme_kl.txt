Startup folder contains ASM files from ST DeviceSupport library. These files
belong to TrueStudio toolchain. All is the same as with files for Ride (both of
them use GCC), but TrueStudio's ones contain additional line, initializing
static constructors.